services:
  # ── MongoDB ─────────────────────────────────────────────────────────────
  mongodb:
    image: mongo:7.0
    container_name: crewbook-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: crewbook_db
    volumes:
      - mongodb_data:/data/db
    networks:
      - crewbook-net
    # Do NOT expose port externally in production; backend connects internally
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    deploy:
      resources:
        limits:
          memory: 512m

  # ── Backend (FastAPI) ────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: crewbook-backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    environment:
      # Override MONGO_URL to use the Docker service name
      MONGO_URL: mongodb://mongodb:27017
      ENV: production
    depends_on:
      mongodb:
        condition: service_healthy
    ports:
      - "8002:8001"
    networks:
      - crewbook-net
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/api/health')"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          memory: 512m

  # ── Frontend (React → nginx) ─────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Leave empty so nginx proxy handles /api — no hard-coded URL in the bundle
        REACT_APP_BACKEND_URL: ""
    container_name: crewbook-frontend
    restart: unless-stopped
    ports:
      - "3002:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - crewbook-net
    deploy:
      resources:
        limits:
          memory: 128m

volumes:
  mongodb_data:
    name: crewbook_mongodb_data

networks:
  crewbook-net:
    name: crewbook-network
    driver: bridge
