# —— Ignore everything that doesn't belong in the Docker image ——
node_modules/
build/
.env
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Keep pnpm-lock.yaml (needed for pnpm install --frozen-lockfile)
# yarn.lock is not needed inside Docker
yarn.lock

# IDE
.vscode/
.idea/
*.swp

# Tests
coverage/
