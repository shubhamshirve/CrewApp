# Frontend environment variables
# Copy this to .env and fill in your values

# Leave empty when using docker-compose (nginx proxies /api to backend)
# Set to your backend URL when running frontend separately (e.g. http://localhost:8001)
REACT_APP_BACKEND_URL=
